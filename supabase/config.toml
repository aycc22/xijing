# 习径 Supabase 本地 / CLI 配置（Edge Functions 等）
# 项目密钥通过 `supabase secrets set` 或 Dashboard 配置，勿写入本文件。

[functions.email-auth]
verify_jwt = false

# JWT 在函数内校验，以便返回统一的 { code: 'unauthorized' }
[functions.ai-proxy]
verify_jwt = false
